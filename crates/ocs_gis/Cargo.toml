[package]
name = "ocs_gis"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Native feature layers with GeoPackage, GeoJSON, and WKB for OpenCADStudio"

[dependencies]
rusqlite = { version = "0.37", features = ["bundled"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
