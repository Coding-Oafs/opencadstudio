[package]
name = "ocs_spatial"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Coordinate reference systems, transformation policy, and provenance for OpenCADStudio"

[dependencies]
proj4rs = { version = "0.1.10", features = ["crs-definitions"] }
crs-definitions = "0.4.0"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
