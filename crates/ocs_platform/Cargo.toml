[package]
name = "ocs_platform"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Integrated transaction, workflow, standards, provenance, and 3D Tiles platform for OpenCADStudio"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
ring = "0.17"
